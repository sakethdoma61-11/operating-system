#include <stdio.h>

int main()
{
    int n, i;

    printf("Enter number of processes: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
        printf("Executing Process P%d\n", i);
    }

    return 0;
}
